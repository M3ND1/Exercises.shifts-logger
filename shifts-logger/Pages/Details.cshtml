@page
@model shifts_logger.Pages.DetailsModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Pages/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>ShiftLogs</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShiftLogs.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShiftLogs.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShiftLogs.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShiftLogs.Description)
        </dd>
        <dt class="col-sm-2">
            @Model.ShiftLogs.StartTime.ToString("dd/MM/yyyy HH:mm")
        </dt>
        <dd class="col-sm-10">
            @Model.ShiftLogs.StartTime.ToString("dd/MM/yyyy HH:mm")
        </dd>
        <dt class="col-sm-2">
            @Model.ShiftLogs.EndTime.ToString("dd/MM/yyyy HH:mm")
        </dt>
        <dd class="col-sm-10">
            @Model.ShiftLogs.EndTime.ToString("dd/MM/yyyy HH:mm")
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ShiftLogs?.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
