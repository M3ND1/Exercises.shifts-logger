@page
@model shifts_logger.Pages.DeleteModel

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Pages/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ShiftLogs</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShiftLogs.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShiftLogs.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShiftLogs.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShiftLogs.Description)
        </dd>
        <dt class="col-sm-2">
            @Model.ShiftLogs.StartTime.ToString("dd/MM/yyyy HH:mm")
        </dt>
        <dd class="col-sm-10">
            @Model.ShiftLogs.StartTime.ToString("dd/MM/yyyy HH:mm")
        </dd>
        <dt class="col-sm-2">
            @Model.ShiftLogs.EndTime.ToString("dd/MM/yyyy HH:mm")
        </dt>
        <dd class="col-sm-10">
            @Model.ShiftLogs.EndTime.ToString("dd/MM/yyyy HH:mm")
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="ShiftLogs.ID" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
